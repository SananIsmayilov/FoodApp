<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/maincolor"
    tools:context=".ui.activity.FoodActivity">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentContainerView2"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        android:layout_marginBottom="60dp"
        app:navGraph="@navigation/foodnavigation" />


    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/btmview"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_gravity="bottom"
        android:layout_margin="10dp"
        android:background="@drawable/bottomviewback"
        app:itemActiveIndicatorStyle="@android:color/transparent"
        app:itemIconSize="25dp"
        app:itemIconTint="@color/bottomappbarselector"
        app:itemRippleColor="@color/white"
        app:itemTextColor="@color/bottomappbarselector"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/menu1" />


</androidx.coordinatorlayout.widget.CoordinatorLayout>